// src/composables/useKunList.js
import { ref, computed, onMounted } from 'vue';
import { isObject, isArray } from '../utils/helpers';

export function useKunList(props, emits, modelValue, items, itemsPerIntersection) {

}